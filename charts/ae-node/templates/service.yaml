apiVersion: v1
kind: Service
metadata:
  name: {{ include "ae_node.fullname" . }}
  labels:
    {{- include "ae_node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ae_node.selectorLabels" . | nindent 4 }}
